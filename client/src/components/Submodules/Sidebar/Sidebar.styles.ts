import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
	sidebar: {
		display: "flex",
		minWidth: "100px",
		boxShadow: "var(--medium-box-shadow)",
	},
});

export { useStyles };
